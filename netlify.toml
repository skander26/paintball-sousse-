# Next.js 14 sur Netlify : le plugin gère le routage SSR/ISR et les assets.
# Ne pas ajouter de redirection SPA « /* → /index.html » : elle casse Next.js.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
